# Codex 실행 프롬프트

아래 작업을 `https://github.com/kuh9325/eastjapanexchange` 저장소에서 수행하라. 저장소가 비어 있으면 처음부터 구현하고, 기존 코드가 있으면 목적에 맞게 정리하되 사용자가 만든 유효한 내용은 보존하라.

## 추론 강도

**High(높음)**으로 수행하라. 단, 구현 자체보다 장황한 조사에 시간을 쓰지 말고 다음 항목에 추론을 집중하라.

1. 행정구역과 한국SGI 방면의 불일치 예외를 정확히 반영하는 데이터 모델
2. 전시용 대형 화면과 터치 조작에 적합한 정보 구조 및 전환 동선
3. 사진이 하나도 없는 상태에서도 완성된 UI가 보이고, 나중에 애셋만 추가하면 자동 반영되는 구조
4. GitHub Pages 정적 배포와 로컬 실행의 재현성
5. 모바일·태블릿·키보드 접근성 및 실패 상태

## 목표

충청남도, 충청북도, 대전광역시 권역의 청년 활동을 지도와 사진 콜라주로 보여주는 전시용 웹앱을 완성하라. 전시 사진 종류는 다음 두 가지다.

- 청년미래총회
- 방문 일대일 근행회

## 반드시 반영할 방면 구역

행정구역 경계와 조직 방면 경계가 다르다. 아래를 단일 진실 원천 데이터로 정의하고 지도와 갤러리 필터 양쪽에서 동일하게 사용하라.

- **대전방면:** 대전광역시, 세종특별자치시, 계룡시, 금산군, 옥천군
- **충남방면:** 충청남도 시·군 중 계룡시와 금산군을 제외한 지역
  - 천안, 공주, 보령, 아산, 서산, 논산, 당진, 부여, 서천, 청양, 홍성, 예산, 태안
- **충북방면:** 충청북도 시·군 중 옥천군을 제외한 지역
  - 청주, 충주, 제천, 보은, 영동, 증평, 진천, 괴산, 음성, 단양

세종은 행정적으로 충남이 아니지만 전시 지도에서 대전방면에 포함한다. 충남 지도에 계룡·금산을 중복 표시하지 말고, 충북 지도에 옥천을 중복 표시하지 마라.

## 지도 및 화면 구성 결정

다음 결정은 이미 확정되었으므로 다시 질문하지 말고 구현하라.

1. 한반도 전체가 아니라 **대한민국 실효 지배 구역의 지도**를 쓴다. 북한까지 포함하면 전시 대상 권역이 너무 작아지고 불필요한 정치적·시각적 주제가 생긴다.
2. 대한민국의 다른 지역은 낮은 대비의 회색 실루엣으로 맥락만 제공한다.
3. 첫 화면에서는 지도를 중앙에 크게 배치한다.
4. 충남·충북·대전 중 하나를 누르면 해당 방면이 3~5% 확대되고 그림자와 굵은 외곽선으로 선택 상태가 명확해진다.
5. 동시에 지도 패널은 왼쪽으로 줄어들고 사진 콜라주 패널이 오른쪽에서 등장한다.
6. 작은 화면에서는 지도 위, 사진 아래의 세로 구조로 전환한다.
7. 세 방면 외 버튼이나 갤러리는 만들지 않는다.

## 지도 표현

- 시·군 단위까지 대략 구분하되 법적 경계 확인용 정밀 GIS 앱으로 만들 필요는 없다.
- 같은 방면은 공통 배경색과 더 굵은 외곽선으로 하나의 집합처럼 보이게 한다.
- 내부 시·군 경계는 얇은 선으로 표시한다.
- 색상만으로 구별하지 않도록 텍스트 라벨, 외곽선, 선택 그림자를 함께 사용한다.
- SVG 기반으로 구현하고 각 방면 그룹은 마우스 클릭, 터치, 키보드 Enter/Space로 선택 가능해야 한다.
- 지도 데이터가 외부 CDN에 의존하지 않도록 저장소 안에 포함한다.
- 공개 경계 데이터를 가져와 사용할 경우 라이선스와 출처를 README에 명시하고, 전시 목적에 맞게 단순화하라. 공개 경계 데이터의 품질이나 최신성이 불확실하면, 무리하게 정밀도를 주장하지 말고 ‘전시용 단순화 지도’임을 명시하라.

## 사진 애셋 구조

정적 웹에서는 브라우저가 폴더 파일 목록을 직접 읽을 수 없으므로, 빌드 전 스크립트가 폴더를 스캔해 manifest JSON을 생성하도록 하라.

```text
assets/gallery/
├─ chungnam/
│  ├─ future/
│  └─ visit/
├─ chungbuk/
│  ├─ future/
│  └─ visit/
└─ daejeon/
   ├─ future/
   └─ visit/
```

요구사항:

- JPG, JPEG, PNG, WebP, AVIF를 지원한다.
- `npm run assets`가 `assets/gallery-manifest.json`을 생성한다.
- `npm run dev`와 `npm run build`가 manifest 생성을 선행한다.
- 사진이 없으면 깨진 이미지가 아니라 완성도 있는 안내용 placeholder 카드를 보여준다.
- 기본 캡션은 파일명을 사람이 읽을 수 있게 정리해 생성한다.
- 각 폴더의 선택적 `captions.json`으로 caption과 alt를 덮어쓸 수 있게 한다.
- 사진을 클릭하면 dialog 기반 lightbox로 크게 보고 이전/다음 이동이 가능해야 한다.

## 갤러리 UX

- 전체, 청년미래총회, 방문 일대일 근행회 탭을 제공한다.
- 비대칭 masonry 느낌의 콜라주를 만들되 외부 라이브러리에 의존하지 않는다.
- 이미지 비율이 다양해도 `object-fit: cover`로 카드가 안정적으로 유지되어야 한다.
- 사진 수, 현재 방면명과 설명을 표시한다.
- ‘전체 지도’ 버튼으로 초기 상태로 돌아갈 수 있어야 한다.
- 과도한 자동 슬라이드나 자동 이동은 사용하지 않는다. 관람자가 주도권을 갖게 한다.

## 기술 조건

- 가능한 한 의존성 없는 HTML, CSS, ES module JavaScript와 Node 빌드 스크립트로 작성한다.
- Node.js 18 이상에서 동작해야 한다.
- `npm run dev`, `npm run build`가 성공해야 한다.
- GitHub Pages용 Actions workflow를 포함한다.
- 상대 경로만 사용해 프로젝트 페이지 하위 경로에서도 동작하게 한다.
- iOS Safari, Chrome, Edge 최신 버전을 고려한다.
- `prefers-reduced-motion`을 지원한다.
- 터치 타깃은 최소 44px 이상으로 한다.
- 접근 가능한 `aria-label`, `aria-selected`, 이미지 alt, dialog 조작을 구현한다.
- 브라우저 콘솔 오류가 없어야 한다.

## 디자인 방향

- 일본 교류 행사에 맞는 따뜻하고 현대적인 전시 디자인
- 미색 종이 질감의 배경, 반투명 패널, 짙은 남색 본문
- 충남은 코랄/주황, 충북은 청록, 대전은 코발트 계열로 구분
- 글꼴은 시스템 한글 폰트 스택을 사용해 외부 폰트 다운로드에 의존하지 않는다.
- 관람 거리를 고려해 제목과 방면명이 크게 보여야 한다.
- 장식보다 사람들의 사진이 주인공이 되게 한다.

## 검증 및 완료 조건

구현 후 다음을 직접 확인하라.

1. 빈 애셋 상태에서 앱이 정상 렌더링되고 안내 카드가 보인다.
2. 각 카테고리에 테스트 이미지 파일을 임시로 넣었을 때 manifest가 생성되고 해당 방면/탭에서만 보인다.
3. 대전방면 선택 시 대전·세종·계룡·금산·옥천이 하나의 선택 상태로 표현된다.
4. 충남방면에는 계룡·금산이 없고 충북방면에는 옥천이 없다.
5. 1440×900, 1024×768, 390×844에서 레이아웃이 무너지지 않는다.
6. 키보드만으로 방면 선택, 탭 이동, lightbox 닫기가 가능하다.
7. `npm run build` 후 `dist/`만 정적 호스팅해도 동작한다.

마지막에 다음을 보고하라.

- 구현한 파일 목록
- 핵심 설계 결정
- 실행 및 사진 추가 방법
- 테스트 결과
- 지도 경계가 단순화된 부분과 향후 실제 GIS 데이터로 교체할 위치
